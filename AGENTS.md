# Godot 项目通用 AI 协作规范

本文件可直接复制到任意 Godot 项目根目录。它只记录稳定的开发习惯、设计边界和工作流程，不记录某个项目当前拥有的目录、模块、场景、版本、输入动作或测试命令。

项目事实应从源码、场景、Resource、`project.godot`、测试和项目说明中获取，并写入项目自己的架构文档或 README，不得回填到本通用文件。

## AI 启动流程

首次进入项目时按需建立工作快照，不要预先读取整个仓库：

1. 检查工作区状态，识别并保留用户已有修改。
2. 读取 `project.godot`，确认实际 Godot 主版本、主场景、Autoload、输入动作、渲染设置和启用插件。
3. 扫描源码、`.tscn`、`.tres`、插件、测试和说明文档，了解真实目录职责，不根据名称猜测架构。
4. 阅读目标文件、所属场景、直接协作者、项目规范及至少一个邻近代表文件。
5. 查找当前项目已有的架构说明、公共用法、测试入口、运行命令和成功条件；不存在时不要假定它们应该存在。
6. 修改公共 API、信号、项目设置、序列化字段或场景挂点前，搜索全部使用方。

开始修改前，至少明确引擎与入口、目标所有者、依赖方向、唯一数据源、验证方式以及仍未确认的风险。除非用户要求，不要仅为保存工作快照而创建文件。

事实冲突时按以下顺序判断：

1. 用户当前任务与明确决策。
2. 实际源码、场景、Resource、项目设置和可重复测试结果。
3. 当前项目的架构说明与使用文档。
4. 与实现一致的邻近注释。
5. 历史审查、路线图、提案和旧说明。

不要把计划写成当前事实，也不要仅根据旧文档删除仍在使用的 API。

## 架构与组合原则

- 优先使用场景树、Node 生命周期、原生 signal、Resource、Group、`process_mode` 和引擎已有节点。
- 引擎已经可靠维护的状态是唯一事实来源，不缓存一份容易分叉的副本。
- 不照搬其他引擎的管理方式；先确认 Godot 的所有权、入树顺序、处理模式和物理语义。
- 原生能力可以清楚完成需求时，不增加同义包装、策略枚举或兼容分支。
- 每项状态只有一个明确所有者；同时明确由谁创建、配置、更新、复位和释放。
- 稳定存在、需要 Inspector 配置或需要开发者观察的结构写入 `.tscn`。
- 静态配置、内容定义和可序列化数据写入 Resource。
- 数量、身份、内容或生命周期只能在运行时确定的对象，以及低成本临时对象，才动态创建或池化。
- 实体根负责共享状态、装配和实体级动作；小型 Component 负责单一局部能力。
- 互斥逻辑适合 State；可以并行启停的能力适合 Behavior 或独立 Component。
- 若使用 Manager，稳定子项作为直属子节点展示，并由 Manager 一次性注入实体和依赖。
- 场景根负责关卡流程、跨实体连接和关卡级依赖；局部能力不升级为全局服务。
- 只有真正跨场景、全局唯一且生命周期明确的能力才使用 Autoload。
- 当前结构可以为 ECS 思维保留数据与职责边界，但没有真实需求时不预建 World、Archetype 或查询系统。
- 依赖由语义所有者显式注入，不通过多层 `../`、整树扫描或隐含命名猜测。
- 一对一协作使用直接调用，局部一对多使用原生 signal；全局事件机制仅用于低频跨系统通知。
- 高频移动、战斗和逐帧状态不经过全局事件总线。
- 规则模型、实体数据和 View 不得分别维护会互相分叉的业务规则。

## GDScript 分组与格式

新建或实质修改脚本时默认采用以下顺序，不存在的部分直接省略：

```text
可选 @tool → extends → 文件级 ## 文档 → 可选 class_name
→ 枚举 → 常量 → 信号 → Inspector 配置 → 依赖引用 → 运行时状态
→ 装配或可覆写回调 → 生命周期 → 主要方法 → 查询方法 → 内部方法
→ 视觉或其他领域专用分组
```

- 使用 `#region 中文名称` / `#endregion`，分组之间和函数之间各留一个空行。
- Region 按职责或领域命名，例如“状态回调”“拖拽”“规则查询”“视觉反馈”，避免“其他”“工具”。
- 极短单职责脚本可以不用 Region；不要制造空分组。
- 只在新建或实质修改时应用规范，不为风格一致性批量重排无关文件。
- 每个函数显式写返回类型；参数、信号参数、节点引用和公共边界尽量使用确定的静态类型。
- 默认值已经清楚表达类型时可用 `:=`；结构稳定时使用类型化容器。
- 状态、事件和语义 ID 优先使用 `StringName`；路径与用户文本使用 `String`；开放载荷才使用 `Variant`。
- 类名使用 PascalCase；变量、方法和信号使用 `snake_case`；内部实现以前导 `_` 标识。
- 遵循项目已有的类名前缀和术语；没有约定时不要擅自为公共 API 发明前缀。
- 校验失败优先早返回；诊断信息使用中文并带具体脚本或类名前缀。

## 注释习惯

- 注释以中文为主，类名、节点名、状态名和 API 名保留英文。
- 文件头使用 `##` 说明脚本负责什么、不负责什么，以及从哪个场景查看组合。
- 信号、Inspector 配置、关键依赖、重要运行时状态和公共方法使用 `##` 说明。
- 公共 API 的文档引用使用 Godot 支持的 `[method]`、`[member]`、`[signal]`、`[constant]`、`[param]` 和 `[code]`；返回语义写在正文，不发明标签，也不滥用排版标记。
- 方法注释重点解释调用时机、所有者、前置条件、副作用和失败结果。
- 方法与信号的说明正文在前，参数说明在后；返回值、失败行为和调用限制写在正文中，不插到参数区之后。
- 有参数时按声明顺序逐项写成 `## [param 参数名] : 说明`。正文与参数区之间使用 `[br][br]`，参数条目之间使用 `[br]`，最后一项不加换行标记；无参数时省略参数区。
- 正文需要明确换行时使用 `[br]`；只是源码折行、仍属于同一句说明时不加。保留原有解释、示例和分组，不因统一格式改写无关内容。
- 方法体中的 `#` 用于标记执行阶段或解释原因，不逐行翻译代码。
- 短阶段注释通常不加句号；完整原因说明使用中文标点。
- 面向入门开发者解释“为什么、何时、由谁使用、避免什么问题”，但不插入无关教程。
- 修改执行顺序、默认值、失败行为或所有权时同步更新注释。
- 不得以“代码已经直观”为由批量删除用户已有注释。

方法注释格式示例（信号采用相同的说明与参数布局）：

```gdscript
## 切换到指定状态。 [br]
## 目标不存在时保持当前状态不变。 [br][br]
## [param state_name] : 目标状态名 [br]
## [param data] : 传递给新状态的数据
func change_state(state_name: StringName, data: Dictionary = {}) -> void:
	pass
```

## 场景表达

- 场景树应直接展示稳定的玩法组成，不在代码中重复动态拼装同一结构。
- 可复用实体使用独立场景；关卡通过实例组合实体，不复制其内部节点。
- 可缩放或旋转的视觉放在独立 VisualRoot 下，避免视觉动画改变碰撞语义。
- 对外稳定挂点可使用 Unique Name；实体内部短且稳定的路径可以使用 `$NodePath`。
- 两端稳定的 signal 连接可以保存在 `.tscn`；运行时实例连接由语义所有者完成。
- signal 处理函数默认命名为 `_on_来源_事件()`，让连接关系可以直接从名称识别。
- `Node.owner` 只表示场景序列化所有权，不把它当作玩法实体归属或依赖发现机制。
- UI/View 只表达输入意图并显示规则结果，不成为第二份规则来源。

## 默认性能边界

- 没有活动时关闭 `_process()`、`_physics_process()` 和不需要的输入处理。
- 事件能够驱动的逻辑不使用永久逐帧轮询；少量计时优先使用原生 Timer 或 SceneTreeTimer。
- 高频路径避免整树搜索、全局广播、重复查询、无意义空回调和临时容器分配。
- 缓存仅用于已确认的高频结果，并明确失效入口；不缓存引擎可可靠查询的状态。
- 对象池只用于重复创建确实产生开销或需要限定实例预算的对象，不默认池化所有短命对象。
- 池化实例依赖正确的 `process_mode` 和原生碰撞停用语义；音频、粒子、Timer、Tween 与业务数据通过取出/归还钩子复位。
- 同一属性的新 Tween 开始前停止旧 Tween；粒子、震动、动态绘制和同屏动画必须有明确预算。
- 先用可重复场景和 Profiler 确认瓶颈，再为热点增加复杂结构，不用复杂度换取无法测量的理论收益。

## 修改与验证

1. 先确认职责、所有者、生命周期和数据来源，再决定脚本或节点位置。
2. 搜索使用方后修改公共接口，保持规则只有一个实现入口。
3. 只完成当前任务需要的最小完整改动，不重写无关模块。
4. 同步更新受影响的场景、注释、测试和项目专属文档。
5. 新抽象应有真实消费者；只有一个用例时优先保持局部实现。

验证时遵守以下规则：

- 从用户环境和项目配置确认 Godot 可执行文件与版本，不硬编码机器路径。
- 优先运行项目已有测试，并遵循其实际成功条件。
- 没有自动化测试时，至少以 headless 模式解析并启动受影响场景。
- 输入、物理、状态切换或异步流程需要验证连续帧和边界顺序。
- 场景切换、保存、对象池等生命周期功能需要覆盖成功、失败、重复调用和离树路径。
- 视觉检查不能替代规则测试；性能结论必须有可复现负载或 Profiler 数据。
- 最后检查差异格式和文件范围，确认没有意外改动、临时探针或生成文件。
